# CLAUDE.md - UI Design Skill

## ⚡️ Usage Guidelines
This skill MUST be consulted effectively whenever the user requests:
- "Design a X page"
- "Create a component for X"
- "Style this..."
- "Make it look good"

## 🛡️ Core Constraints
1.  **NEVER** invent your own colors. You MUST use the palette in `TOKENS.md`.
2.  **NEVER** use default Tailwind shadows. Use the "Flat Depth" rule (color blocking).
3.  **ALWAYS** check `LAYOUT_PATTERNS.md` before designing a full page.
4.  **ALWAYS** check `COMPONENT_PATTERNS.md` before designing buttons or inputs.

## 🔍 Discovery
- **Tokens**: `TOKENS.md` (Start here for variables)
- **Layouts**: `LAYOUT_PATTERNS.md` (Heroes, Navs, Grids)
- **Components**: `COMPONENT_PATTERNS.md` (Buttons, Cards)

## ❌ Common Mistakes
- Using pure white `#FFFFFF` (Use `#F9F7F2` instead).
- Using system sans-serif for headings (Use `Playfair Display`).
- Making the UI too "loud" (Use Orange `#FF6B4A` sparingly).
